pipeline {
  agent any
  stages {
    stage(build) {
      steps {
        echo 'Build hello.sh'i
      }
    }
    stage(test) {
      steps {
        echo 'Test hello.sh'
      }      
    }
    stage(deploy) {
      steps {
        echo 'Deploy hello.sh'
      }
    }
  }
}
